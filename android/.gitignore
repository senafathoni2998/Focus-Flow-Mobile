gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Gradle writes reports here even though android/build.gradle.kts relocates the real
# output to the Flutter project's build/ — and the root .gitignore's /build/ is anchored
# to the repo root, so it does not cover this one.
/build/
/.kotlin/
