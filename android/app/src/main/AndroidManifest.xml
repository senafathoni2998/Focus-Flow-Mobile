<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Every screen talks to your self-hosted backend; without this the app
         builds fine and then fails every request with a connection error. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Android 13+ requires an explicit grant before a notification can be
         shown. The app asks at runtime (NotificationService.init), but the
         prompt never appears unless the permission is declared here, so due
         reminders would stay silent with no visible cause. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="FocusFlow"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <!-- usesCleartextTraffic: the dev backend is plain http:// on a LAN
             address. REMOVE THIS once you terminate TLS in front of the backend
             — as written it permits cleartext to ANY host, which on a shared
             network means bearer tokens travel in the clear.

             allowBackup=false: tokens live in flutter_secure_storage, which is
             Keystore-backed and therefore DEVICE-BOUND. Android auto-backup
             would copy the encrypted blobs to a new device where the key does
             not exist, and that decryption failure is exactly what used to pin
             the app on its splash screen forever. Excluding the app from backup
             removes the failure mode at its source rather than catching it. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Share-to-app: puts FocusFlow in the Android share sheet for text,
                 so a link or a line from a chat becomes a task without leaving
                 the app you were in.

                 SEND only, not SEND_MULTIPLE: several shared items would each
                 need a decision (one task or many? which title?) and guessing
                 wrong creates junk the user then has to clean up.

                 The activity is launchMode="singleTop", so a share into a
                 running app arrives at onNewIntent rather than starting a second
                 copy with its own empty state. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Home-screen widget. The provider is a BroadcastReceiver belonging to
             this app, so its code runs in THIS process and can read the snapshot
             the app writes to SharedPreferences — no token, no network, and no
             Flutter engine spun up per redraw. -->
        <receiver
            android:name=".FocusFlowWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/focusflow_widget_info"/>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
